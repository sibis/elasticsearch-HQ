import angular from 'angular';
import indexMetricSegment from './index-metric-segment.component';

export default angular.module('eshq.indexMetricSegment', [])
  .component('eshqIndexMetricSegment', indexMetricSegment)
  .name;
