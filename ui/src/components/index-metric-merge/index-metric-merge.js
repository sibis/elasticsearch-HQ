import angular from 'angular';
import indexMetricMerge from './index-metric-merge.component';

export default angular.module('eshq.indexMetricMerge', [])
  .component('eshqIndexMetricMerge', indexMetricMerge)
  .name;
